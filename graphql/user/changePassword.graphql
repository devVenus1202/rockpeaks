mutation($currentPassword: String!, $newPassword: String!) {
  updateUserPassword(current_password: $currentPassword, new_password: $newPassword) {
    entity {
      entityId
    }
    violations {
      message
    }
    errors
  }
}
